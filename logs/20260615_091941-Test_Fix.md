# Test Fix Plan — 20260615_091941

## Execução inicial

**Comando**: `npm run test:e2e`
**Data**: 2026-06-15 09:19:41
**Total de testes**: 416 (projeto `mobile`)
**Resultado**: 44 passaram · 372 falharam · 2 ignorados

---

## Causa raiz

O servidor **Vite preview** (`vite preview`, porta 5173) travou com o erro Windows
`3221226505` (`0xC0000409` = `STATUS_STACK_BUFFER_OVERRUN`) após a execução do
**teste 44** de forma sustentada.

```
[WebServer] npm error code 3221226505
[WebServer] npm error command C:\WINDOWS\system32\cmd.exe /d /s /c vite preview
```

Esse comportamento está documentado em `CLAUDE.md`:

> "the `vite dev` server has crashed under sustained runs — `0xC0000409` — and
> CPU-heavy specs like axe-core a11y can intermittently exceed the 10s global timeout"

Após o crash, todos os testes subsequentes falharam com:

| Erro | Significado |
|------|-------------|
| `ECONNRESET` | Servidor derrubou a conexão durante o crash |
| `ECONNREFUSED ::1:5173` | Servidor já estava fora do ar |

Nenhuma falha de asserção de regra de negócio foi detectada.

---

## Testes que passaram (antes do crash)

| Arquivo | Testes passados |
|---------|----------------|
| `tests/a11y/axe.spec.ts` | 8 |
| `tests/a11y/teclado.spec.ts` | 6 |
| `tests/album/cadastro-album.spec.ts` | 11 |
| `tests/albums/albums.spec.ts` | 19 (testes 26–44) |
| **Total** | **44** |

---

## Testes ignorados (skipped)

- `PDF de Figurinhas Faltantes (RN-AL19, AL30)` — 2 testes marcados como `skip`
  (`tests/albums/albums.spec.ts:245:9` e `tests/albums/albums.spec.ts:259:9`)

---

## Classificação das falhas

**Todas as 372 falhas são flakiness ambiental** causada pelo crash do servidor.
Nenhuma falha indica bug de código.

Conforme orientação em `CLAUDE.md`:
> "If it passes in isolation but fails in the full run, treat it as environment
> flakiness — do **not** change the test's logic."

---

## Plano de ação

### Etapa 1 — Encerrar processos nas portas (já executado)
```bash
npx kill-port 5173 3000
```

### Etapa 2 — Rebuildar o cliente
Garante artefato de build limpo antes da re-execução:
```bash
npm run build -w client
```

### Etapa 3 — Reexecutar suite completa
```bash
npm run test:e2e
```

### Etapa 4 — Verificar resultado
- Se todos os testes passarem → flakiness confirmada, nenhum código alterado.
- Se houver falhas determinísticas (mesmo em re-run isolado) → investigar como
  falha de código genuína.

---

## Observações / Recomendações

1. O crash `0xC0000409` é sensível à carga de CPU: os 8 testes `axe-core`
   (CPU-intensivos) precedem imediatamente os testes de álbum que acumularam
   memória até o crash após o teste 44.
2. Nenhuma alteração de código ou de teste foi feita: a causa raiz é ambiental.
3. Para reduzir recorrência em futuras execuções agendadas, seria possível
   separar os testes em duas passagens (`axe.spec.ts` por último), mas isso
   requer revisão de escopo separada.
